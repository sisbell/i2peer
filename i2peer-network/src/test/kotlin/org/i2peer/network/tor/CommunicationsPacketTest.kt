package org.i2peer.network.tor

import org.junit.jupiter.api.Test

class CommunicationsPacketTest {

    @Test
    fun encodeDecode() {
       // val process = Process("myId", "ikvh2uz76knum6dw")
       // val message = Message("Hello Onion".toByteArray())
       // val communicationsPacket = CommunicationsPacket(process, message, 4)
      //  val encoded = communicationsPacket.encode()
    //    encoded.flush()

        //val input = ByteArrayInputStream(encoded.buffer().readByteArray())
        //val result = CommunicationsPacket.read(Okio.buffer(Okio.source(input)))
       // println(result.targetProcess.port)

    }
}